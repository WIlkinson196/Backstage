export type VenueContext = {
  userId: string | null;
  userName: string;
  organisationId: string;
  organisationName: string;
  venueId: string;
  venueName: string;
  role: string;
  demoMode: boolean;
};

