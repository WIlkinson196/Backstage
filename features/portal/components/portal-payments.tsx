import type { PortalPayment } from "@/features/portal/types/portal";
import { CheckCircle2, Clock3, CreditCard } from "lucide-react";

export function PortalPayments({payments}:{payments:PortalPayment[]}) {
  const due=payments.filter(p=>p.status==="due").reduce((s,p)=>s+p.amount,0);
  return <div className="grid gap-6 xl:grid-cols-[340px_1fr]"><section className="rounded-3xl bg-[#101822] p-6 text-white"><div className="text-[10px] font-bold uppercase tracking-[.14em] text-[#D8B77D]">Remaining balance</div><div className="mt-3 font-serif text-4xl">£{due.toLocaleString("en-GB",{minimumFractionDigits:2})}</div><p className="mt-3 text-sm leading-6 text-white/45">Your venue team will confirm the agreed payment schedule. Online payment can be enabled through Stripe later.</p><button className="mt-6 inline-flex items-center gap-2 rounded-xl bg-white px-4 py-3 text-xs font-semibold text-[#101822]"><CreditCard size={15}/> Pay securely</button></section><section className="rounded-3xl border border-[#E8E0D6] bg-[#FFFCF8] p-6"><h2 className="font-serif text-3xl">Payment schedule</h2><div className="mt-5 divide-y divide-[#E8E0D6]">{payments.map(p=><div key={p.id} className="grid gap-3 py-4 md:grid-cols-[1fr_120px_130px] md:items-center"><div className="flex gap-3">{p.status==="paid"?<CheckCircle2 size={17} className="text-[#5D7A62]"/>:<Clock3 size={17} className="text-[#B6915D]"/>}<div><div className="text-sm font-semibold">{p.label}</div><div className="mt-1 text-xs text-black/35">{p.date}</div></div></div><div className="font-semibold">£{p.amount.toLocaleString("en-GB",{minimumFractionDigits:2})}</div><div className="text-xs capitalize text-black/40">{p.status}</div></div>)}</div></section></div>
}
